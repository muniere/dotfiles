function addpath() {
  # delete duplicate path
  local base=$PATH
  for x in "$@"; do
    base=${base//$x/}
    base=${base//::/:}
  done

  # add new path
  local args="$@"
  export PATH="${args// /:}:$base"
}

# vim: ft=sh sw=2 ts=2 sts=2
