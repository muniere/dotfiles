function addpath() {
  # delete duplicated path
  local args="$@"
  local base=$PATH
  for x in $args; do
    base=${base//$x/}
    base=${base//::/:}
  done

  # add new path
  local ext="${args// /:}"

  export PATH="$ext:$base"
}

# vim: ft=sh sw=2 ts=2 sts=2
